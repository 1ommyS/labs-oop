#pragma once
#include "vector.h"

int charToNumber(char number);

unsigned char numberToChar(unsigned int num);

int threeToDecimal(Vector<unsigned char> digits);

std::string decimalToTernary(int decimal);